spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/draft_predict
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: java2905

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true